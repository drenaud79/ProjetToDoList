

const action = [{
  }];

export const UseReducerList = (state, action) => {
    switch (action.type)
    {
        case 'AddItem':
            return ;    
        case 'DeleteItem':
            return ;
    }

    return action;
}